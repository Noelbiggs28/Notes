Math:
// rounds down and divides
% remainder
** power

can concat lists
list3 = list1 + list2

can do 2 < x < 7
instead of 2 < x and x < 7

python uses not instead of !         if not x < 7:       ==      if(!x < 7)

sets a default argument
print(sum(num1, num2=10))

[start, stop, step]
word='hello'
word[::2] = hello
word[::-1] = olleh
word.find('3') returns 1 returns index of letter

min() and max() finds in an array
word.sort(). alters in place
sorted(word) makes a sorted copy of the list

list comprehension
numbs = [1,2,3,4,5]
double_nums = [2 * num for num in nums]
only_evens = [num for num in nums if num % 2 == 0]

tuples are immutable. can be used as keys
capitals{
    'Washington':'W'
    'Ohio':'O'
}
for (key, value) in capitals.items():
print(key,value)
for num in enumerate(numbs):
    print(num)